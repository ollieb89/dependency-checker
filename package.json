{
  "name": "dependency-checker",
  "version": "1.0.0",
  "description": "GitHub Action to scan for outdated and vulnerable dependencies",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "node src/test.js"
  },
  "keywords": ["github-actions", "dependencies", "security", "vulnerability"],
  "author": "Ollie B",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.0"
  }
}
